[core]
	editor = vim
[user]
	name = Orlando Tomás
[credential]
	helper = cache --timeout=3600
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[pull]
	rebase = true
